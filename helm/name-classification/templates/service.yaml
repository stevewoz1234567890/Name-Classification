apiVersion: v1
kind: Service
metadata:
  name: {{ include "name-classification.fullname" . }}
  labels:
    {{- include "name-classification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "name-classification.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
