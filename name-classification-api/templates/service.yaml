apiVersion: v1
kind: Service
metadata:
  name: {{ include "name-classification-api.fullname" . }}
  labels:
    {{- include "name-classification-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "name-classification-api.selectorLabels" . | nindent 4 }}
