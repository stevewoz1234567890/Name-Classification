apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "name-classification-api.fullname" . }}-test-connection"
  labels:
    {{- include "name-classification-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "name-classification-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
